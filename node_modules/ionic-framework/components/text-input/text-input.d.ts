import { ElementRef, Renderer, EventEmitter } from 'angular2/core';
/**
 * @private
 */
export declare class TextInput {
    private _elementRef;
    private _renderer;
    private _relocated;
    type: string;
    ngModel: any;
    value: string;
    focusChange: EventEmitter<boolean>;
    valueChange: EventEmitter<string>;
    constructor(type: string, _elementRef: ElementRef, _renderer: Renderer);
    /**
     * @private
     */
    ngOnInit(): void;
    /**
     * @private
     */
    private _keyup(ev);
    /**
     * @private
     */
    private _focus();
    /**
     * @private
     */
    private _blur();
    labelledBy(val: string): void;
    /**
     * @private
     */
    setFocus(): void;
    /**
     * @private
     */
    relocate(shouldRelocate: boolean, inputRelativeY?: any): void;
    /**
     * @private
     */
    hideFocus(shouldHideFocus: boolean): void;
    hasFocus(): boolean;
    /**
     * @private
     */
    addClass(className: string): void;
    hasClass(className: any): boolean;
    /**
     * @private
     */
    element(): HTMLElement;
}
